version: '3.9'
services:
  chromadb:
    image: chromadb/chroma:0.5.5
    container_name: rag-chroma
    ports:
      - "8000:8000"
    volumes:
      # 🌟 核心魔法：把数据挂载到项目根目录的 .chroma_data 文件夹下
      - ./.chroma_data:/chroma/chroma
    restart: unless-stopped